# docs/specs/

Implementation specs written by the **component-architect** agent (one file per component,
`<component>.md`). Each spec drives the build pipeline — see
[`../../.claude/PIPELINE.md`](../../.claude/PIPELINE.md).

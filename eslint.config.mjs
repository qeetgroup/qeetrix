// Root lint covers repo-level scripts/config. Packages and apps have their own
// flat config that extends @qeetix/eslint-config (base or react preset).
import { base } from "@qeetix/eslint-config";

export default base;

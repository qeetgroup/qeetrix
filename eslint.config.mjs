// Root lint covers repo-level scripts/config. Packages and apps have their own
// flat config that extends @qeetrix/eslint-config (base or react preset).
import { base } from "@qeetrix/eslint-config";

export default base;

---
"@qeetrix/tokens": patch
---

Align `font.family` tokens with the design system's actual fonts: `sans` → **Cal Sans Text**, `heading` → **Cal Sans Display**, plus a new `ui` → **Cal Sans UI** family and a Cal-Sans-based `mono` stack (previously these pointed at generic system stacks).
